# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Alternative: Use gpt-4 for better responses (costs more)
# OPENAI_MODEL=gpt-4

# Redis Configuration (for stateful mode)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your_redis_password  # Uncomment if Redis requires authentication

# Session Configuration
SESSION_TTL_SECONDS=3600  # Session expiration time (default: 1 hour)
